# Local development MongoDB (runs with docker-compose)
spring.data.mongodb.uri=mongodb://localhost:27017/upi_backend_db
spring.data.mongodb.database=upi_backend_db

# Keep debug logging for local runs
logging.level.org.springframework.data.mongodb=DEBUG
logging.level.org.mongodb.driver=DEBUG
