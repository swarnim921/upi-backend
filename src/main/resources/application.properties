spring.application.name=upi-backend
server.port=8080
logging.level.root=INFO
logging.level.org.springframework=DEBUG



# Mongo connection (fallbacks keep local dev simple)
spring.data.mongodb.uri=${MONGODB_URI:mongodb+srv://HIMA6768:Hima2005.@cluster0.rydrkx3.mongodb.net/}
spring.data.mongodb.database=${MONGODB_DATABASE:upi_dashboard}

# JWT configuration
jwt.secret=${JWT_SECRET:this-is-a-placeholder-secret-change-me}
jwt.expiration-minutes=60

# Google OAuth2 configuration
# Note: Spring Security OAuth2 uses default redirect URI: /login/oauth2/code/google
# Make sure to add this exact URI to your Google OAuth2 client's authorized redirect URIs
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET}
spring.security.oauth2.client.registration.google.scope=openid,profile,email

# Frontend Configuration
app.frontend.url=${FRONTEND_URL:http://localhost:5173}
